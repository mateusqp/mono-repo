global:
  namespace: app-prod

ingress:
  enabled: true
  hosts:
    - host: app.example.com
      paths:
        - path: /
          service:
            component: frontend
            port: 80
  tls:
    - secretName: app-prod-tls
      hosts:
        - app.example.com
