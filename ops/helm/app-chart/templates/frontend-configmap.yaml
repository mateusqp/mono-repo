{{- if and .Values.frontend.enabled .Values.frontend.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app-chart.componentName" (list . "frontend-config") }}
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    {{- include "app-chart.labels" (list . "frontend") | nindent 4 }}
data:
  {{- if .Values.frontend.config.data }}
  {{- toYaml .Values.frontend.config.data | nindent 2 }}
  {{- else }}
  {}
  {{- end }}
{{- end }}
