global:
  namespace: app-hom

ingress:
  enabled: true
  hosts:
    - host: app.hom.example.com
      paths:
        - path: /
          service:
            component: frontend
            port: 80
  tls:
    - secretName: app-hom-tls
      hosts:
        - app.hom.example.com
